import React, {FC} from 'react';
import {Box} from "@mui/material";

interface MainProps {
}

const Main: FC<MainProps> = () => {
    return (
        <>
            <Box>

            </Box>
        </>
    );
};

export default Main;