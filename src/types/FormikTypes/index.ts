export interface IFormDataAuth {
    email: string;
    password: string;
}

export interface IFormDataReg {
    email: string;
    password: string;
    full_name: string;
    position: string;
}

export interface IFormFeedback {
    name: string;
    email: string;
    question: string;
}